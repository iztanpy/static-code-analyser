procedure Example {
  x = k - (1 + i) * 3 / 2 - 0;
  x = k - 1 + i * 3 / 2 - 0;
  x = k - 1 + (i * 3) / 2 - 0;
  x = (k - 1) + i * (3 / 2) - 0;
  z = k - 1 + (i * 3) / 6;
  i = (6 / z) - 2 + x - (1 * i);
  y = k - 1 + i * 3 / 2 - 0;
  y = z;
  i = z + 1 - x;

  while (i != (0 * 5)) {
    x = x - (1 * 1) + (0 / 2);
    if (x == (2 - 1)) then {
      z = x + 2 + 1 - 1;
    }
    else {
      y = z + x - 0 + (0 * 3);
    }
    z = z + x + i - 2 + 2;
    call sumDigits;
    i = i - 1 + 2 - 2;
  }
  x = z;
  call sumDigits;
}

procedure sumDigits {
    read x;
    read i;
    z = (0 * i) + 0;

    while (x > (y - 5)) {
        t = z + 1 - x;
        y = x % 10 * 10 + i + 0 * z - 0 + (1 + i);
        z = z + y - (0 * 1);
        x = k / (5 + 5) * i;

        if (x >= (5 + 5)) then {
            print i;
        } else {
            while (x <= (9999 + 1)) {
                y = x - (5 * 2) + z - 1 + (k-1);
            }
            print x;
        }
    }
    print z;
}