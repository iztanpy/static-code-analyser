name: CI for C++ linting
on: [pull_request]
jobs:
  cpplint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-python@v1
    - run: pip install cpplint
    - run: |
        cd Team16/Code16/src/spa
        cpplint --recursive extensions=cpp,h .